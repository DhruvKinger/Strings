#include <bits/stdc++.h>
using namespace std;
int main()
{
    char s[]="Jauaxuxxxx";
    map<char,int> mp;
    int n=sizeof(s)/sizeof(s[0]);
    int j=0;
    string x;
    for(int i=0;i<n;i++)
    {
        mp[s[i]]++;
    
    }
    
    for(auto i:mp)
    {
        if(i.second>1)
        {
            std::cout << i.first << std::endl;
        }
    
    }
    return 0;
}
